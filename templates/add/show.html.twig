{% extends 'base.html.twig' %}

{% block title %}Add{% endblock %}

{% block body %}
    <h1>Add</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ add.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ add.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ add.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('add_index') }}">back to list</a>

    <a href="{{ path('add_edit', {'id': add.id}) }}">edit</a>

    {{ include('add/_delete_form.html.twig') }}
{% endblock %}
