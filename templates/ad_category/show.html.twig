{% extends 'base.html.twig' %}

{% block title %}AdCategory{% endblock %}

{% block body %}
    <h1>AdCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ad_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ad_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ad_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ad_category_index') }}">back to list</a>

    <a href="{{ path('ad_category_edit', {'id': ad_category.id}) }}">edit</a>

    {{ include('ad_category/_delete_form.html.twig') }}
{% endblock %}
