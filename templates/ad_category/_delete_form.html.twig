<form method="post" action="{{ path('ad_category_delete', {'id': ad_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ad_category.id) }}">
    <button class="btn">Delete</button>
</form>
