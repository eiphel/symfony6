
{% form_theme form with [
    'bootstrap_5_horizontal_layout.html.twig'
] %}

{% form_theme form with [
    '_form/theme.html.twig'
] %}


{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
